/*
    -> Tipagem implícita
    - Ao declarar uma variável e não declarar o tipo, e sim só o conteúdo que ela recebe, o typescript entende que a tipagem daquela variável será o tipo de dado recebido pela variável.
*/

let userName = 'Thomaz'

// Ao declarar a variável userName com dado do tipo string, o typescript entende que a tipagem para aquela variável sera do tipo String.